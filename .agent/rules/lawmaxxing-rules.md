---
trigger: always_on
---

Always use shadcn compoenents. Never shard code the shadcn components, use the latest npm to install them.

When making changes to Supabase Schema, create a helper file where you keep track of what is done, so you do not lose the context of current database setup.